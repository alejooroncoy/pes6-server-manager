---
import { Icon } from "astro-icon";
import SoccerGoal from "../SoccerGoal.astro";
import Link from "../Link.astro";
---

<section class="hero">
  <div class="hero-container">
    <article class="content">
      <div class="content-container">
        <h2
          class="content__title"
        >
          Pes 6 server manager
        </h2>
        <p class="content__paragraph">
          Pes6 server manager helps its users to be able to have control of the
          servers where they play. In this way, with a few simple clicks, play
          PES 6 on the server of your choice. 💻⚽
        </p>
        <div class="content__buttons">
          <Link
            modifier="dark"
            href="https://psm.up.railway.app/psm/bin?version=0.0.1"
            target="_blank"
            className="flex gap-1 items-center justify-center bg-primary"
            rel="noopener noreferrer"
          >
            <Icon class="text-white w-5 h-5" name="bx:bxs-cloud-download" />

            Download Version Bin
          </Link>
          <Link
            rel="noopener noreferrer"
            href="https://github.com/alejooroncoy/pes6-server-manager"
            className="flex gap-1 items-center justify-center bg-secondary p-2"
            target="_blank"
          >
            <Icon class="text-black w-5 h-5" name="bx:bxl-github" />
            Github Repo
          </Link>
        </div>
      </div>
    </article>
    <SoccerGoal className="w-full h-fit md:w-auto" />
  </div>
</section>

<style>
  .hero {
    @apply min-h-screen pt-20;
  }
  .hero-container {
    @apply p-4 box-border flex items-center justify-center gap-1 flex-col md:flex-row;
  }
  .content-container {
    @apply py-4 flex justify-center flex-col gap-2;
  }
  .content__title {
    @apply text-3xl sm:text-4xl m-0 lg:text-6xl h-auto pb-2 leading-none xl:text-7xl font-extrabold text-transparent bg-clip-text from-primary to-thirdary bg-gradient-to-br;
  }
  .content__paragraph {
    @apply text-lg m-0 text-slate-600 max-w-xl;
  }
  .content__buttons {
    @apply m-0 flex flex-col sm:flex-row gap-3;
  }
</style>
